# Development Configuration
# Loaded on top of base.yaml for development environment

server:
  debug: true
  reload: true
  workers: 1

logging:
  level: DEBUG

extensions:
  auto_discover: true
  auto_load: true

# Enable all development-friendly features
custom_settings:
  enable_debug_endpoints: true
  mock_llm: false
  verbose_logging: true
